name: installation of nginx 



on:
  workflow_dispatch:
    inputs:
      triggerworkflow:
        description: 'installing the software'
        required: true
        default: "" 


jobs:
  install-nginx:
    runs-on: ubuntu-latest

    steps:
      - name: setup nginx
        run: |
          sudo apt-get update -y
          sudo apt-get install -y nginx

  check-nginx-service:
    runs-on: ubuntu-latest

    steps:
      - name: printing the statement 
        run: |
           echo "Successfully installed :${{ github.event.inputs.triggerworkflow }}"

